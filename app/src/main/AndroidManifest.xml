<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Fuer Live-Spracherkennung ueber das Mikrofon (ML Kit GenAI Speech Recognition). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      Nur fuer den einmaligen on-device Modell-Download (Uebersetzungs- und
      Spracherkennungsmodelle) benoetigt. Es werden zu keinem Zeitpunkt
      Kundendaten (Text/Audio) an einen Server uebertragen - siehe README.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Paketsichtbarkeit (Android 11+): noetig, damit die App den
      Spracherkennungsdienst des Systems sehen und binden kann
      (SystemSpeechEngine, android.speech.SpeechRecognizer - zweite
      Live-Engine fuer Sprachen ohne ML-Kit-Abdeckung).
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
